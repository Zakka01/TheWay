# MazeGen
A Python-based algorithmic maze generator and solver. Features include random maze generation (perfect and non-perfect), pathfinding for the shortest valid route, and custom visualization (ASCII or MLX). This project includes a reusable, pip-installable module for procedural maze generation
